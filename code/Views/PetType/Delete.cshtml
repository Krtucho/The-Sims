@model WebApplication1.Models.PetType
@*@{ var title = "Create PetType"; }*@

<form method="post">
    @*@if (Model.PetType.PetTypeID != 0)
        {
            title = "Edit PetType";*@
    <input asp-for="PetTypeID" hidden />
    <div class="border p-3">

        <div class="form-group row">
            <h2 class="text-info pl-3">Delete</h2>
        </div>
        <div class="row">
            <div class="col-8">
               
                <div class="form-group row">
                    <div class="col-4">
                        <label asp-for="PetTypeID">@Model.PetTypeID</label>
                    </div>
                    <div class="form-group row">
                        <div class="col-8 offset-4 row">

                            <div class="col">
                                <button type="submit" class="btn btn-danger w-100"> Delete</button>
                            </div>
                            <div class="col">
                                <a asp-action="Index" class="btn btn-success w-100"><i class="fas fa-sign-out-alt"></i> Back</a>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-4">
                    @* Keep this empty *@
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts{

    @{
                        <partial name="_ValidationScriptsPartial" />
    }

}